{
  "build": {
    "publish": {
      "root": "build",
      "artifactLocation": "build"
    },
    "appLocation": "public",
    "apiLocation": "api",
    "outputLocation": "build",
    "timeout": "10m"
  }
}
